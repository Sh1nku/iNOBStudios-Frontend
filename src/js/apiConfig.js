export const API_PROTOCOL = import.meta.env.VITE_API_PROTOCOL;
export const API_SERVER = import.meta.env.VITE_API_SERVER;
// host is unreliable, and is not set if Http/2 request, therefore hostname needs to be set
export const HOSTNAME = import.meta.env.VITE_HOSTNAME;
